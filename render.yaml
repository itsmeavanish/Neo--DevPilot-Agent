services:
  - type: web
    name: neo-devpilot-api
    runtime: docker
    dockerfilePath: ./Dockerfile.cloud
    plan: free
    envVars:
      - key: PORT
        value: "8000"
      - key: PYTHONPATH
        value: /app/src
